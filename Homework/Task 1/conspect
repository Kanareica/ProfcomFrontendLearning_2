код подключается <script type="text/javascript" src="путь к файлу"></script>
структура синтексиса похожа на Pascal
setTimeout(function, time) - вызвать function через time ms
alert("text") - всплывающее окно с текстом
переменные получают тип при занесении данных
Типы в JS:
    number - число, не обязательное целое
    bigint - большие целые числа, в конце числа нужен n
    string - строка
    boolean - логический тип
    null - для неизвестных значений
    undefined - специальный тип для неприсвоенных значений
    symbol - уникальный id
    object - объект(сложные структуры данных)
важен регистр
переменные начинаются с букв, $ или _
